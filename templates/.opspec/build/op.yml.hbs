name: build
description: builds the op
run:
  serial:
    - op:
        ref: github.com/opspec-pkgs/_.op.bootstrap#{{bootstrapOpVersion}}
        inputs:
          srcDir: $(../..)
    - op:
        ref: $(../test)